pipeline {
    agent {
        docker {
            image 'alpine:latest' // Imagem Docker a ser usada como agente (Alpine Linux neste exemplo)
            label 'my-docker-agent' // Opcional: rótulo personalizado para o agente Docker
        }
    }
    
    stages {
        stage('Echo Hello Mundo') {
            steps {
                sh 'echo "Hello Mundo"'
            }
        }
    }
}