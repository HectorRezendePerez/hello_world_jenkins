pipeline {
    agent {
        docker {
            image 'alpine:latest' // Imagem Docker a ser usada como agente (Alpine Linux neste exemplo)
            // args '-v /path/to/workspace:/var/jenkins_home' // Argumentos adicionais do contêiner Docker (opcional)
        }
    }
    
    stages {
        stage('Echo Hello World') {
            steps {
                sh 'echo "Hello World"'
            }
        }
    }
}